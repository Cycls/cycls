syntax = "proto3";

package runtime;

service Runtime {
  rpc Execute(Request) returns (stream Response);
}

message Request {
  bytes payload = 1;
}

message Response {
  bytes data = 1;
  bool error = 2;
}
